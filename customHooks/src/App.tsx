// just like useState , useEffect , useCallback , we can write our own hooks
// only naming convention is that their name should start with use

export function App() {
  return <></>;
}

export default App;
